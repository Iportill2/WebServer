

-Chequear en reads y writes que si se retorna un error el cliente es borrado.

-Chequear en reads y writes que el valor de retorno se chequea correctamente  (no vale chequear solo -1 o 0 hay ke chekear ambos)

-Asegurarse de los codigos de respuesta estan bien.

-Mirar bien todo el tema de "curl" (lo que viene en la hoja de correccion y mas)

-Controlar el tamaño del body

-Implementar delete

-chequear cgi con GET y POST??????(lo que contaba Iban sobre el env. en execv???)

--Chequear cgi erroneo, loop infinito por ejemplo

-que pasa con el root del server??? lo ponemos por defecto para las locations que no tengan root o que es lo correcto?

-Un Makefile como dios manda?? (cada cosita en su carpetita...)

-Si no hay archivo de configuracion tiene que cojer uno por defecto, no lo ke hemos hecho de setearlo en Server.

-El archivo de configuracion debe decir donde se guardan los archivos ke se suben!!!
